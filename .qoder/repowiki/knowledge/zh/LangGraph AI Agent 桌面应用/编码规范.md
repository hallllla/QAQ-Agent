- IPC 通信遵循 `ipcMain.handle` (主进程) 与 `ipcRenderer.invoke` (预加载/渲染进程) 的请求-响应模式，事件通知则使用 `webContents.send` 配合 `ipcRenderer.on`。
- Agent 工具定义统一采用 `@langchain/core/tools` 的 `tool()` 辅助函数，结合 `zod` 库进行输入参数的 Schema 校验与类型定义。
- 渲染进程组件通过全局注入的 `window.electronAPI` 对象访问底层能力，避免直接引用 `electron` 模块以维持上下文隔离安全性。